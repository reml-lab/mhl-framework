# mhealthlab-server-containers

This repo contains docker-compose specifications for containers to run mHealthLab streaming data services. 

* Use "docker-compose up" to build and run the server containers.
